# Directors Dealings — outstanding backlog (snapshot 2026-05-29)

**Compiled:** 2026-05-29, at the close of Sprint 14 (cohort / CAR-chart redesign).
**Purpose:** a single review list of everything deliberately deferred, requested,
or surfaced-but-not-actioned during Sprint 14, so Rupert can decide what (if
anything) to pull into a future sprint. Nothing here is a blocker — the core
redesign shipped and is gated. This complements the running `docs/backlog.md`;
items here are net-new or re-confirmed as of today.

**B-number block:** Sprint 14 used B-064–B-071. New items below are assigned
**B-072 onward** (provisional — confirm before scoping).

---

## Sprint 14 — what shipped (for context)

Phases 0–6 of the cohort/CAR-chart redesign are built and passed their QA gates:

- **Phase 0** — interim mean-vs-median divergence warning on the performance table.
- **Phase 1** — Level-1 design (sparkline + trend + focus-mode), completed by the designer.
- **Phase 2** — `cohort_performance.json` export (per-month cohort aggregates, rolling-6m hit rate, drill-down data). Includes the **metric-correction fix**: the single-ticker / verdict / drill-down "share" now uses bounded `|r_i|/Σ|r_j|` ("Share of cohort movement") instead of the sign-inverting contribution-to-mean.
- **Phase 3** — Level-1 performance-table augmentation: Trajectory sparkline + 3-month trend column + focus-mode row click.
- **Phase 4** — Level-2 cohort chart: scatter means, min/max whiskers, N strip, single-ticker "!" marker, 3-month MA overlay, dynamic y-axis, **calendar-true x-axis with gap months shown**.
- **Phase 5** — rolling-6-month hit-rate panel + on-chart legend/key.
- **Phase 6** — drill-down modal: click a dot → per-trade table (CARs, benchmark, net, share of cohort movement) + plain-English verdict.
- Plus: removed the **legacy 12-week sparkline** column (superseded by Trajectory).

---

## Outstanding items

### Cohort-chart enhancements

| ID | Item | Why it matters | Rough effort | Notes / dependencies |
|----|------|----------------|-------------|----------------------|
| **B-072** ✅ RESOLVED 2026-06-03 | **Horizon toggle (T+1 / T+21 / T+90 / T+252)** on the cohort charts. | Sprint 24: `_LEVEL2_MONTH_KEYS` extended to thread all four-horizon fields (data was already in `cohort_performance.json`); T+1/T+21/T+90/T+252 toggle buttons added to focus overlay; hit-rate panel header made dynamic. JS `cohortPick()` + `horizonChange` listener were already in place — Python data thread was the only gap. |
| **B-073** | **Small-multiples grid** — all 11 signals as thumbnail cohort charts side-by-side. | Faster cross-signal scan than clicking each row. Deferred from the brief as a candidate v2. | Medium | Brief said "evaluate after living with Level 1 + Level 2 for a few weeks." Decide if the Trajectory sparklines already cover this need. |
| **B-074** | **Multi-signal overlay** in the Level-2 chart (compare 2–3 signals on one chart). | Direct visual comparison of trajectories. | Medium | Brief deferred this — overlapping dots/whiskers across many signals is visually messy. Only worth it for a small, user-selected subset. |
| **B-075** ✅ RESOLVED 2026-06-03 | **CSV export of the drill-down table.** | Sprint 24: floating "↓ Download CSV" button on all drill-down pages. Client-side JS reads embedded `#drillData` payload at current horizon × lookback, serialises rollup to CSV, triggers browser download. |
| **B-076** ✅ RESOLVED 2026-06-03 | **Persist selected signal + horizon** in localStorage. | Sprint 24: `_cohort_focus_script()` saves group + horizon on change. On load, restores state and auto-reopens focus mode if saved group still exists in data blob. |
| **B-077** | **Mobile / narrow-screen pass (<600px)** for the performance page + cohort charts. | Dashboard is desktop-first; charts don't reflow on a phone. | Medium | Explicitly out-of-scope in the brief. Only if you want to check the app on mobile. |

### Methodology / correctness

| ID | Item | Why it matters | Rough effort | Notes |
|----|------|----------------|-------------|-------|
| **B-078** | **Volume-weighted cohort means** (weight each signal by the £ value of the director's purchase, not 1-trade-1-vote). | A £5m CFO buy plausibly carries more conviction than a £20k one; equal-weighting may understate that. | Medium–Large | Real methodology question — affects **every CAR number** across the dashboard, not just this chart. Needs its own spec + a decision on whether to change the headline numbers or show it as an alternate view. Agents: trader + analyst + backend. |
| **B-079** | **Confirm the focus-view header "mean" definition.** | The on-chart header strip computes an N-weighted mean over months; the export also carries `mean_car_t21_overall`. Minor inconsistency. | Trivial | QA-flagged as cosmetic. One-line confirm with the designer on which is intended, then align. |

### Signal taxonomy / model findings (strategic — for your judgement)

These came out of the model assessment during the redesign. They are **investment-strategy** calls, not UI work — flagged so they're not lost.

| ID | Item | Why it matters | Notes |
|----|------|----------------|-------|
| **B-080** | **Review chronically negative signals at T+90.** The assessment showed most signals with N≥20 were negative at T+90 over the current 12-month regime. | Several "buy-the-insider" signals are not adding edge in this regime. | Decide per signal: keep / deprecate / down-weight. The new chart + drill-down now give you the per-month evidence to judge. Possible single-regime artefact — re-check as data grows. Agents: analyst + trader. |
| **B-081** | **F1 (first-time buy) deprecation candidate.** Large N (~700+) and near-zero/negative mean — the most statistically trustworthy "no edge" reading in the set. | Strong candidate to retire or down-weight. | Watch it on the live chart for a few weeks first. |
| **B-082** | **S1 (cluster buy) — confirm status.** Flagged anti-predictive; Sprint 13's B2 suppression was meant to blunt its worst cases. | Verify whether S1 still fires meaningfully post-B2, and whether it earns its place. | Tie to the Sprint 13 behavioural-signals work. |

### Tech debt / cleanup

| ID | Item | Why it matters | Rough effort | Notes |
|----|------|----------------|-------------|-------|
| **B-083** ✅ RESOLVED 2026-06-03 | **Remove dead `render_helpers.sparkline_svg`.** | Sprint 24: function deleted from `render_helpers.py`; `test_stage_05.py` test removed. `cohort_sparkline_svg` (the production replacement) unchanged. |
| **B-084** ✅ RESOLVED 2026-06-04 | **Audit `hidden`-attribute + Tailwind display-class combos across the dashboard.** | Sprint 27: exhaustive regex scan of all render_*.py files — CLEAN. No `hidden` attribute co-occurs with `flex` class. `overflow-hidden` is CSS overflow (safe). cohortFocus uses `hidden` + `fixed` (safe). Tabs use class toggle (safe). No fixes needed. |
| **B-085** ✅ RESOLVED 2026-06-03 | **Add a lightweight browser smoke-check to the QA loop.** | Sprint 24: 277 Python tests green in sandbox. Manual browser checklist: open focus mode → switch each horizon → click drill-down → download CSV → reload (check localStorage restore). |

### Pre-existing open items (still relevant — from `docs/backlog.md`)

| ID | Item | Notes |
|----|------|-------|
| B-058 | Replace non-cp1252 Unicode (`→ ✓ ✗ ⚠`) in `check_announced_at_coverage.py` / `repair_pending_review.py` `print()`s with ASCII. | Trivial; avoids a `UnicodeEncodeError` if either gets wired into a subprocess-captured pipeline step. |
| B-059 | Company finder search box on the dashboard (jump to any of ~560 company pages by name/ticker). | Usability; mostly client-side JS over the already-exported company index. |

---

## Suggested priority (Rupert's call)

- **Highest user value:** B-072 (horizon toggle) — it's the one that changes how you read every signal. Biggest effort too.
- **Quick wins:** B-075 (CSV export), B-076 (persist selection), B-083 (dead-helper cleanup), B-059 (company search).
- **Strategic / do-when-ready:** B-080/B-081/B-082 (signal taxonomy review) — these are about *which signals to trust*, and the new chart finally gives you the evidence to decide.
- **Process improvement:** B-084 / B-085 — cheap insurance against the class of bug that bit us twice this sprint.

## How to use this file

Bring it up when planning the next session: "anything from the 2026-05-29 backlog worth pulling in?" Promote chosen items into a sprint plan (PM agent), leave the rest. Re-confirm B-numbers against `docs/backlog.md` before scoping so they don't collide.

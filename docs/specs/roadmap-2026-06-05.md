# Directors Dealings — Roadmap update 2026-06-05

**Date:** 2026-06-05  
**Status:** LIVE — Sprints 26–31 fully closed 2026-06-05. Product-improvement sprints 33–36 (B-107–B-116) scoped 2026-06-05 — see §8.  
**Supersedes:** `roadmap-2026-06-04c.md`

> **⚡ ACTIVE EPIC — Cloud Migration (2026-06-22):** moving the whole project off the local PC to **GitHub + Supabase + Vercel** (autonomous sprint-runner build). Its roadmap/board is the dedicated tracker [`cloud-migration-sprint-tracker.md`](cloud-migration-sprint-tracker.md) (plan/ADR/execution-plan alongside). Linear: milestones "Cloud Migration — M0..M5", issues **B-172→B-189 (DIR-101→118)**. Status as of 2026-06-22: **M0 + M1 DONE** (904k rows live in Supabase, parity verified), **M2 in progress** (pipeline port). This work is tracked separately from the feature roadmap below.

---

## 1. Canonical state — what has shipped

| Date | What |
|------|------|
| 2026-06-05 | Sprint 31 CLOSED: B-077 mobile/responsive pass (header nav truncate + flex-shrink-0, table overflow-x-auto + min-w-[640px]/min-w-[700px]); B-073 small-multiples signal grid on performance page (12 buy-signal thumbnail Chart.js charts, horizonChange listener, click-to-focus). 22 new tests green. |
| 2026-06-04 | Sprint 30 CLOSED: CSV seed (market caps 46→78 rows, websites 48→80 rows); B-002 LLM sweep (2/35 recovered, 33 confirmed bundled_multi_PDMR); B-078 Phase B deferred. Hotfixes: sqlite3.Row .get() crash, run_pending_sweep missing export step, test_sprint29 fixture fix. 207 bash tests green. |
| 2026-06-04 | Sprint 29 CLOSED: B-104/B-079/B-098/B-009/B-019/B-103/B-102/B-100 Phase B. 32 tests green. |
| 2026-06-04 | Sprint 28 CLOSED: B-096b/B-097b/B-101b/B-100 Phase A/B-078 VW toggle. 38 tests green. |
| 2026-06-04 | Sprint 27 CLOSED: B-090 Layout A+B. 24 tests green. |
| 2026-06-04 | Sprint 26 CLOSED: B-105/B-097/B-096/B-101. Schema v8. 27 tests. |
| 2026-06-04 | Sprint 25 CLOSED: apply_edits/delete/audit-log/Audit & Undo. 77 tests. |
| 2026-06-03 | Sprint 24 CLOSED: B-083/B-072/B-076/B-099/B-075. 277 tests. |
| 2026-06-03 | Sprint 22 CLOSED: B-091/B-094/B-092. 166 tests. |
| Earlier | Sprints 10–14/20: pipeline resilience, CAR chart, IT purge, parser hardening, role taxonomy. |

---

## 2. Open backlog

### 2a. Already-scoped work

| ID | Item | Sprint assigned | Size |
|----|------|----------------|------|
| **B-078 Phase B** | Promote VW means as default view. **Gated:** t1b=22, b1=23, t6=5 — all below ≥30 T+21 threshold. Also needs VW added to `_build_horizon_aggregates()` payload. Revisit Q3 2026. | Sprint 32+ (gated) | M |
| **B-074** | Multi-signal overlay in Level-2 chart. | Sprint 32+ | M |
| **B-090 Layout C** | Multi-tranche SIP layout (33 bundled_multi_PDMR confirmed unrecoverable by LLM sweep). | Sprint 32+ | M |
| **B-080** | Review chronically negative signals at T+90. | Signal review Q3 2026 | — |
| **B-081** | F1 (first-time buy) deprecation candidate. | Signal review | — |
| **B-082** | S1 post-B2 status confirm. | Signal review | — |
| **B-107–B-116** | Product-improvement scoping 2026-06-05 (absolute-return column, This-Week rename, per-tier hit-rate, signal-live indicator, upcoming-earnings calendar, monthly-activity depth, cluster score, earnings multiplier, conviction sizing, push digest). | Sprints 33–36 — see §8 | — |
| **B-117** | Test-suite health — repair 26 pre-existing full-sweep reds (stale assertions/fixtures). Discovered Sprint 32; **folded into Sprint 33 as the opening job** so the gate is trustworthy before display wins land. Linear DIR-40. | Sprint 33 | M |
| **B-145** | **Upcoming Events box — This Week page.** Rolling 2-week forward look at reporting calendar events sourced from `reporting_dates`. Displays ticker, company name, event type (interim/full/AGM), date. Sorted date-ascending (nearest first). Zone A: `export_dashboard_json.py` + `render_index.py`. Linear DIR-72. | Sprint 53 | S |

### 2b. Tech debt queue — **moved into Sprint 35 (2026-06-05)**

The whole tech-debt queue was folded into Sprint 35 on 2026-06-05 (Rupert's call). No longer unscheduled. See §8 Sprint 35.

| ID | Item | Size | Status |
|----|------|------|--------|
| B-001 | Multi-row bulk filings (rows 2–N dropped) | M | ✅ already shipped Sprint 3 |
| B-013 | DB connection-leak audit | S | → Sprint 35 |
| B-016/17 | Parser boilerplate / director-name in company field | S | → Sprint 35 |
| B-020 | Triage 334 orphan candidates from Sprint 3 reparse | M | → Sprint 35 |
| B-021 | `classify_issuers.py` resets flags on every run | S | → Sprint 35 |
| B-022 | Empty company name fallback | S | → Sprint 35 |
| B-023 | Bundled-PDMR detection misses AAL "PCA" pattern | S | → Sprint 35 |
| B-060 ✅ | Pence/pounds audit + fix (migration 010 price_audit) — **P1** | M | DONE 2026-06-06 |
| B-090C | Multi-tranche SIP parser (deferred low-ROI) | M | → Sprint 35 |

---

## 3. Completed B-numbers (reference)

| ID | Item | Sprint |
|----|------|--------|
| B-001 ✅ | Multi-row bulk filings (rows 2–N) — table-aware parser emits one row per data row | Sprint 3 (2026-05-18); confirmed already-shipped during Sprint 32 triage 2026-06-05. 159 parser tests green. |
| B-074 ✅ | Multi-signal overlay in Level-2 chart — finished the dormant B-019 overlay path (hoisted monthLabel, fixed activeH scope bug, horizon-aware re-render, single-group chrome teardown, discoverability hint). Also cleared 5 pre-existing Sprint-24 test reds. | Sprint 32 (2026-06-05). 65 tests green on Windows. |
| B-002 ✅ | LLM sweep over stuck-on-date pending filings | Sprint 30 — 2/35 recovered; 33 bundled_multi_PDMR confirmed unrecoverable |
| B-009 ✅ | CAR sparkline: 12 monthly buckets, null forward-fill | Sprint 29 |
| B-019 ✅ | CAR chart toggle/solo mode | Sprint 29 |
| B-043 ✅ | Non-cp1252 Unicode audit | Sprint 27 — CLEAN |
| B-059 ✅ | Company search box | Earlier |
| B-072 ✅ | Horizon toggle T+1/T+21/T+90/T+252 | Sprint 24 |
| B-073 ✅ | Small-multiples signal grid (performance page) | Sprint 31 |
| B-075 ✅ | CSV drill-down export | Sprint 24 |
| B-076 ✅ | localStorage signal + horizon persistence | Sprint 24 |
| B-077 ✅ | Mobile / narrow-screen pass | Sprint 31 |
| B-078 ✅ (Phase A) | VW cohort means toggle | Sprint 28 |
| B-079 ✅ | Focus-view mean label confirmed + tooltip | Sprint 29 |
| B-083 ✅ | Remove dead sparkline_svg | Sprint 24 |
| B-084 ✅ | Hidden+flex audit | Sprint 27 — CLEAN |
| B-085 ✅ | Browser smoke-check | Sprint 24 |
| B-090 ✅ | Bundled multi-PDMR recovery (Layout A+B) | Sprint 27 |
| B-091 ✅ | Historical backfill | Sprint 22 |
| B-092 ✅ | Non-RNS provider parser fixes | Sprint 22 |
| B-093 ✅ | UTL/UIL NED-buy recovery | Sprint 20 |
| B-094 ✅ | announced_at non-ISO fix | Sprint 22 |
| B-095 ✅ | Split guard (Cornish Metals) | Sprint 20 |
| B-096 ✅ | Reporting dates schema + badge | Sprint 26 |
| B-096b ✅ | Reporting dates Investegate data source | Sprint 28 |
| B-097 ✅ | Market cap + shares_outstanding schema | Sprint 26 |
| B-097b ✅ | Market cap CSV fallback | Sprint 28 |
| B-098 ✅ | Absolute stock return column (Today + Company pages) | Sprint 29 |
| B-099 ✅ | B1 cohort chart row + B2 suppression count | Sprint 24 |
| B-100 Phase A ✅ | Read-only paper book display | Sprint 28 |
| B-100 Phase B ✅ | Paper trade write path (eval + close) | Sprint 29 |
| B-101 ✅ | Website link on company page | Sprint 26 |
| B-101b ✅ | Website URL CSV fallback | Sprint 28 |
| B-102 ✅ | Monthly buy/sell summary chart | Sprint 29 |
| B-146 ✅ | Earnings date chart markers + "(est)" badge + tooltips | Sprint 32 (2026-06-09) |
| B-147 ✅ | Earnings History box on company page + migration 012 source_url | Sprint 32 (2026-06-09) |
| B-148 ✅ | Fix 594 corrupted reporting_dates rows (bad June 4th scraper URL) | Sprint 32 (2026-06-09) |
| B-103 ✅ | Sortable table headers (Today page) | Sprint 29 |
| B-104 ✅ | Value filter — all buy signals | Sprint 29 |
| B-105 ✅ | AIM All-Share benchmark (^AIM) | Sprint 26 |
| B-106 ✅ | SyntaxWarning docstring | Sprint 27 |
| CSV seed ✅ | `_company_market_caps.csv` + `_company_websites.csv` top-50 active tickers | Sprint 30 |

---

## 4. Agent perspectives

### 4a. Quant Researcher

**On B-073 (small-multiples — shipped):**
The thumbnail grid gives an at-a-glance view of all 12 buy signals' monthly CAR trajectory on one screen. Useful for spotting signals that are trending better/worse recently before drilling into focus mode. The horizonChange wiring means the thumbnails update when the user switches T+1/T+21/T+90/T+252 — no stale data on horizon switch.

**On B-078 Phase B (VW default — still gated):**
t1b=22, b1=23, t6=5 at T+21 — still below ≥30. ETA: ~Q3 2026. Additionally requires `vw_mean_car` added to the `horizon_aggregates` payload (currently only in cohort cuts / cohorts_v2).

**On B-078 Phase B (paper trades):**
`paper_trades` table is live. `close_paper_trades.py` closes at T+21/T+90. Run monthly. Once ≥30 closed trades exist, compare paper P&L to CAR to validate model.

### 4b. Token-Efficiency Engineer

Sprint 31 was pure Zone A (HTML/JS/Python render changes). Zero LLM spend, zero DB writes. The small-multiples grid reuses existing `window.__cohortData` already in the page — no new backend data needed.

---

## 5. Sprint plan — Sprints 31–33 (product-improvement sprints 33–36 detailed in §8)

### Sprint 31 ✅ CLOSED 2026-06-05

| ID | Item | Result |
|----|------|--------|
| B-077 ✅ | Mobile/responsive pass — all pages. | Done — header truncate + flex-shrink-0, table overflow-x-auto + min-w. |
| B-073 ✅ | Small-multiples signal grid (performance page). | Done — 12 buy-signal thumbnail charts, horizonChange listener, click-to-focus. |

**Rupert deploy sequence:**
```powershell
python .scripts\export_dashboard_json.py
python .scripts\build_dashboard.py
```
*(No schema changes, no backfill needed — pure render rebuild.)*

---

### Sprint 32 — Candidates

| ID | Item | Size |
|----|------|------|
| ~~**B-074**~~ ✅ | Multi-signal overlay in Level-2 chart. **Shipped Sprint 32 2026-06-05** — finished the dormant B-019 overlay path; 65 tests green. | M |
| ~~**B-090 Layout C**~~ ⏸ | Multi-tranche SIP parser fix (33 confirmed bundled_multi_PDMR). **DEFERRED 2026-06-05** → Tech Debt queue, priority Low. All 33 recovered rows are NON_BUY SIP tranches (partnership/matching/free shares) — zero buy-signal value, larger build (new C2 matrix extractor). Revisit when signal work is quiet. | M |
| ~~**B-001**~~ ✅ | Multi-row bulk filings (rows 2–N dropped). **Already shipped Sprint 3** — confirmed during Sprint 32 triage 2026-06-05; closed without rebuild. | — |
| ~~**B-146**~~ ✅ | Earnings date chart markers on company share price charts — vertical dashed lines + diamond scatter + "(est)" badge suffix. **Shipped Sprint 32 2026-06-09** — Linear DIR-79. | M (3) |
| ~~**B-147**~~ ✅ | Earnings History box on company page + migration 012 `source_url`. **Shipped Sprint 32 2026-06-09** — `_filings_section()`, schema v12, backfill + build pipeline updated. Linear DIR-80. | M (3) |
| ~~**B-148**~~ ✅ | Fix 594 corrupted `reporting_dates` rows (bad June 4th scraper URL). **Shipped Sprint 32 2026-06-09** — one-shot cleanup script, 6 legitimate rows preserved. Linear DIR-81. | S (1) |

---

## 6. Decisions locked (all still valid)

| # | Decision |
|---|----------|
| 1 | B-072 / horizon toggle — confirmed same feature, no new ID |
| 2 | Paper P&L position sizing — conviction-scaled (notional = director's £ value). Cap £50k (CLI arg `--max-notional`). |
| 3 | B-105 CAR diff review before dashboard update — non-optional gate |
| 4 | B-096 closed-period badge — 60-day window |
| 5 | B-100 Phase A before Phase B — both now shipped |
| 6 | B-090 bundled recovery — Layout A+B done Sprint 27; Layout C deferred to Sprint 32 |
| 7 | reparse_corpus 57% gate — use `--force-safety-override` after reviewing `--preview` output |
| 8 | B-098 baseline on Today page = T+1 close (signal entry); on Company page = transaction deal price |
| 9 | B-019 single-group mode = full detailed chart; multi-group = simplified overlay (means only) |
| 10 | B-078 Phase B gate — ≥30 T+21 firings required per signal before VW promoted as default. Also requires VW added to `_build_horizon_aggregates()` payload (currently cohort-cuts only). |
| 11 | B-073 b2 exclusion — b2 (Crowded Cluster Kill) is a suppression signal, not a directional buy. No thumbnail card in the overview grid; same treatment as scoreboard sparkline (shows suppression count instead). |

---

## 7. Signal taxonomy review (deferred)

Criteria: 30+ post-cleanup firings per signal at T+90. Check monthly; revisit Q3 2026.
B-080 (negative signals), B-081 (F1 deprecation), B-082 (S1 post-B2) all on hold.

---

## 8. Product-improvement sprints (Sprints 33–36) — 2026-06-05 scoping

These are **Sprints 33+**, following the pencilled **Sprint 32** (tech-debt candidates B-074, B-090 Layout C, B-001 — left as-is, not touched here). New B-IDs allocated **B-107 → B-116**, one per discrete scoping item. Full per-item detail lives in `product-improvements-2026-06-05-plan.md` and (for #5) `upcoming-earnings-dates-plan.md`. Original scoping pencilled these as Sprints 32–35; renumbered to 33–36 so the pre-existing Sprint 32 tech-debt slot is preserved untouched.

### Item → B-ID mapping

| Scoping item | B-ID | One-line user need | Sprint | Size |
|---|---|---|---|---|
| #1 — Absolute return column next to CAR | **B-107** | As a market-maker, I want the raw stock return shown beside each CAR figure, horizon-matched, so a good relative number isn't mistaken for money made | 33 | S–M |
| #6 — Remove Today box, keep/rename "This Week" | **B-108** | As a daily reader, I want the empty Today table gone and the page named "This Week" so I'm not staring at dead weight | 33 | S |
| A — Per-tier live hit-rate | **B-109** | As a signal-trader, I want each tier's historical % positive (with n) so I can tell which tiers actually work | 33 | M |
| E — Signal "still live?" indicator | **B-110** | As a reader, I want to see how much of a signal's actionable window remains so I don't chase stale signals | 33 | S |
| #5 — Upcoming earnings dates (LSE Diary) | **B-111** | As a trader, I want a forward earnings calendar so the 60-day pre-results badge actually lights up | 34 | L |
| #3 — Monthly activity click-through + 12mo totals + trend | **B-112** | As a market-watcher, I want to drill into monthly buy/sell blocks and see whether insider buying is accelerating | 35 | M |
| C — Cluster conviction score | **B-113** | As a signal-trader, I want active clusters ranked by a single conviction score so the strongest cluster surfaces first | 35 | M |
| B — Earnings-proximity signal multiplier | **B-114** | As a trader, I want a buy landing just before earnings scored as a stronger signal so I can prioritise it | 36 | M |
| D — Buy size vs skin-in-the-game | **B-115** | As a trader, I want each buy sized against the director's prior holding so I can tell conviction from noise | 36 | M |
| F — Morning push digest | **B-116** | As a busy trader, I want a 7am email of overnight director buys so the signal finds me | 36 | M |

*(Scoping item #2 — sort toggles — was DROPPED; sortable headers already shipped B-103. Scoping item #4 — remove model-assessment box — locked as KEEP; B-109 ADDS hit-rate alongside it, not as a replacement.)*

### Sprint 33 — Test health + cheap display wins (data already computed)

Goal: **first** make the gate trustworthy (B-117), then ship the four items whose data already exists in the export, for fast visible value with zero new data engineering. ~9 points. Linear milestone: *Sprint 33 — Test Health + Cheap Display Wins* (cycle 2).

**Sequencing:** B-117 runs **first** — a clean full `unittest discover` sweep is the gate the four display wins are then verified against. (B-117 has no hard code-dependency on B-107–B-110; it's sequenced first so every subsequent QA gate this sprint is honest.)

| ID | Item | Size |
|----|------|------|
| **B-117** ✅ | **DONE 2026-06-05 — full sweep green (1149 tests OK, was 26 red); zero production code changed.** Test-suite health (folded in 2026-06-05, Linear DIR-40). Repaired the 26 pre-existing full-sweep reds (19 fail + 7 error) — stale assertions/fixtures across ~8 files (schema-version pins, cohort export shape, Sprint-24 dynamic-horizon string drift, `prices.fetched_at`, render_company/review.html evolution, sparkline/paper-close/mean_abs_return). Update tests to match current code; **never** change production output to make a test pass without separate sign-off. Adopt the full sweep — not per-file runs — as the gate. Expands/supersedes B-012. | M (3) |
| **B-107** ✅ | **Already shipped (verified 2026-06-05).** Abs-return column beside CAR: `_matured_abs`/`mean_abs_return` in export; "Abs Rtn" column + JS horizon-lockstep in render_performance scoreboard. (Main "This Week" table has no horizon toggle, uses fixed `abs_return_pct` from B-098.) | S–M (2) |
| **B-108** ✅ | **Already shipped.** `_sort_today` removed (zero callers), `<title>` "Directors Dealings - This Week", signals-today tile retained. | S (1) |
| **B-109** ✅ | **Already shipped.** `_hit_rate_panel` wired into render() alongside the kept model-assessment box; low-N (n<10) de-emphasis; updates on horizonChange. | M (2) |
| **B-110** ✅ | **Already shipped.** `signal_live_chip` (live 0–30 / ageing 31–89 / closed ≥90d) in render_helpers, attached in render_index. | S (1) |

**Sprint 33 status: COMPLETE 2026-06-05.** B-117 fixed (gate green, 1149 tests); B-107–B-110 found already-shipped during scoping and verified green in the full sweep (test_sprint33.py). No new code; already live via the B-074 export+build. The "silently-completed backlog" pattern again (cf. B-001).

**Acceptance criteria**
- B-117: `python -m unittest discover -s .scripts -p "test_*.py"` returns OK (or every remaining red is explicitly justified + ticketed). Each fix is an assertion/fixture update, not a production-behaviour change (any genuine code regression found is flagged separately, not silently patched). ResourceWarning DB-leak noise is out of scope (that's B-013).
- B-107: abs return per row equals `close_at_horizon / entry − 1`; toggling T+1/T+21/T+90 moves the **CAR and Abs columns together** (lockstep verified); header reads "Abs (since txn, T+21)" etc. so the window is unambiguous. A known sector-down fixture shows +CAR / −Abs. (If per-horizon closes aren't yet exported, add `abs_return_t1/_t21/_t90` in `export_dashboard_json` → bumps this to M.)
- B-108: page renders This-Week table only, no empty Today block; `<title>` and visible H2 both read "This Week"; "Signals today" tile still present; `_sort_today` orphan handled (grep callers before delete). Updated `render_index` tests pass.
- B-109: panel rows match `horizon_aggregates` / `hit_rate_t21` for a fixture; updates on horizon toggle; tiers with n<10 visibly de-emphasised (reuse existing low-N styling); shows n beside every %.
- B-110: state mapping correct at boundary ages 0 / 89 / 90 / 200 days.

**Out of scope:** any new signal_id (none of these introduce one); the earnings badge surfaces (Sprint 34).

**Rupert deploy sequence:**
```powershell
python .scripts\export_dashboard_json.py
python .scripts\build_dashboard.py
```
*(Add the export step only if B-107 needs new per-horizon closes; B-108/B-110 are pure render, B-109 reads existing aggregates.)*

### Sprint 34 — Forward earnings dates (the one real data job)

Goal: stand up a forward-looking earnings calendar from the lse.co.uk Financial Diary so the 60-day pre-results badge lights up across all relevant boxes. Isolated into its own sprint because it carries the new Zone-B script + schema migration.

| ID | Item | Size |
|----|------|------|
| **B-111** ✅ | New Zone-B scraper `backfill_lse_diary.py` + schema **migration 009** (`confidence` col + widened `report_type` CHECK to 7) + 60-day badge extension. **DONE 2026-06-05** (1164 tests green). **Sprint 34 complete.** | L (5) |

**B-111 — DONE 2026-06-05 (gate green, 1164 tests):**
- ✅ Migration 009 (`009_reporting_dates_confidence.sql` + `db.py` chain, schema head now **"9"**): rebuilds `reporting_dates`, adds `confidence` (NOT NULL DEFAULT 'confirmed'), widens `report_type` to INTERIM/FINAL/TRADING_UPDATE/EARNINGS + PRELIM/QUARTERLY/TRADING_STMT.
- ✅ `backfill_lse_diary.py` — **month-view** scrape (one fetch/month — leaner than weekly), real URL `/share-prices/financial-diary.html?selected-date=DD-Mon-YYYY&mode=month`. Parser validated against committed fixture `.scripts/fixtures/lse_diary_sample.html` (June). Maps Final→PRELIM, Interim→INTERIM, Q1-Q4→QUARTERLY, Trading Announcement→TRADING_STMT; ignores AGM/dividends/economic. Held-ticker filter, replace-on-rerun, polite UA+retry+24h cache, unmatched-TIDM coverage log.
- ✅ `test_lse_diary.py` (12 tests) + 2 schema-head pin tests bumped 8→9.
- ✅ First run: **130 results dates written, 115/599 held tickers covered**; company-page badges live.
- ✅ **This-Week / main-table badge**: `export_dashboard_json.build_dealings` attaches `near_reporting_date` + `near_reporting_est` to today/this-week rows; `render_index._row_html` renders the amber `~results` chip (+ `(est)` when `confidence='est'`). +3 render tests.
- ⏭ **Est-filler split out to B-118** (DIR-41, Sprint 36): `backfill_expected_reporting_dates.py` synthetic "(est)" gap-filler — deferred per Rupert 2026-06-05.
- Horizon default bumped to **6 months** (current+6) so the daily run keeps a rolling forward window. Note: the Diary currently publishes confirmed dates only through ~Sep 2026 (Oct–Dec return 0 until companies announce); the daily run picks them up as published, and B-118 would estimate the gaps.

**Acceptance criteria**
- Pure parser unit test against a **committed sample** of diary HTML extracts the right `(date, TIDM, report_type)` tuples and ignores AGM / dividend / economic rows. Type map: Final→`PRELIM`, Interim→`INTERIM`, Q1–Q4→`QUARTERLY` (or folded — see open question), Trading Announcements→`TRADING_STMT`.
- Ticker normalisation unit test covers the awkward cases (`.` suffixes, dual lines like YNGA/YNGN, preference codes); unmatched-TIDM log produced for coverage audit.
- Read-only integration against a `/tmp` DB copy reports coverage (# held tickers with ≥1 confirmed future date).
- Migration 009 is additive only: existing rows default `confidence='confirmed'`; `build_dashboard`'s `report_date >= today` query is unchanged and now picks up confirmed future rows.
- Badge shows the date and appends "(est)" only when `confidence='est'`, so a synthetic date never reads as confirmed.
- Scrape is polite: identified UA, rate-limit, retry/backoff, short cache TTL (~24–48h, not 30 days).

**Out of scope:** Phase B "Notice of Results" RNS fallback (ship only if Diary gaps prove material); the synthetic "(est)" gap-filler `backfill_expected_reporting_dates.py` (open question 4); idea B multiplier (Sprint 36, depends on this landing).

**Rupert deploy sequence:**
```powershell
python .scripts\fetch_sectors.py
python .scripts\backfill_ticker_meta.py
python .scripts\backfill_benchmarks.py
python .scripts\backfill_reporting_dates.py     # historical (unchanged)
python .scripts\backfill_lse_diary.py           # NEW — confirmed FUTURE dates
python .scripts\export_dashboard_json.py
python .scripts\build_dashboard.py
```
*(Migration 009 runs via the project's normal schema-migration path before the first `backfill_lse_diary.py` run. `backfill_lse_diary.py` is Zone B — Rupert runs it; Claude never does.)*

### Sprint 35 — Monthly-activity depth + cluster ranking + tech-debt sweep — ✅ COMPLETE 2026-06-06

Goal: turn two descriptive surfaces into signals — make the monthly chart trend-aware and drillable, and rank active clusters by conviction. **All items closed (verified vs code 2026-06-06): the two features + the whole tech-debt sweep were already built (silent-completion pattern) or triaged; only B-090 Layout C remains and is pushed to a future parser-coverage sprint.**

| ID | Item | Size |
|----|------|------|
| **B-112** ✅ | Monthly activity drill + trailing-12 buy/sell totals + £-trend glyph. Built: `export_dashboard_json.build_monthly_buysell` + `render_performance._build_monthly_buysell_section` (trend chips, click-drill panel); tests `test_sprint34.py`. *(Minor deviations: drill is an embedded panel not a cohort_drilldown reuse, no monthly-CSV button, trend is vs-prev-3m not strict month-over-month — flag if strict spec parity wanted.)* | M (3) |
| **B-113** ✅ | Cluster conviction score — `compute_active_clusters` conviction = directors×3 + value-tier×2 + compression×2; sorted desc; `cv:` chip in render_index; tests `test_sprint34.py:TestConvictionScore`. | M (3) |

**Tech-debt sweep (folded into Sprint 35 2026-06-05) — ✅ ALL CLOSED 2026-06-06.** Reconciled vs code: B-060 shipped; the parser/classifier/hygiene items were all already-built; B-020 is a deliberate triaged-closed; only B-090 Layout C remains and was moved out to a future parser-coverage sprint with the new B-121.

| ID | Tech-debt item | Pts | Status |
|----|------|-----|--------|
| **B-060** ✅ | Pence/pounds audit + fix (migration **010** `price_audit`; `price_reconcile.py` + `backfill_price_units.py`; 1 `corrected_pence`, 607 flagged+nulled; head→"10"). | 3 | DONE + DEPLOYED 2026-06-06 |
| **B-013** ✅ | DB connection-leak audit — production scripts pair `db.connect()` with `try/finally: close()` (refresh_all/backfill_filings et al). *(The test-suite ResourceWarning flood is test-fixture noise, explicitly out of B-013 scope.)* | 1 | ALREADY-BUILT |
| **B-016/17** ✅ | Boilerplate / director-name in `company` field — `_is_boilerplate_company` + cross-row validation in parse_pdmr; folded into the Sprint-3 table-aware rewrite. | 1 | ALREADY-BUILT |
| **B-021** ✅ | `classify_issuers` sticky exclusion (Source E reads `_excluded_it_cef.csv`; flags never auto-cleared); `test_classify_sticky_flag.py`. | 1 | ALREADY-BUILT |
| **B-022** ✅ | Empty-company Pass-3 fallback in `_find_company_in_soup`. | 1 | ALREADY-BUILT |
| **B-023** ✅ | Bundled-PDMR PCA pattern in `_bundled_name_warning`; `test_b023_bundled_sections.py`. | 1 | ALREADY-BUILT |
| **B-020** ✅ | Triage 334 orphans — `_diag_orphan_retriage.py` found 107 residual orphans are REAL filings (PCA/corp-holder + SIP/EBT), NOT garbage → do NOT delete; real fix escalated to **B-121**. | 3 | TRIAGED-CLOSED |
| **B-090C** ⏭ | Multi-tranche SIP parser Layout C. **Moved out of Sprint 35** → Tech-Debt queue, grouped with **B-121** for a future "Sprint 38 — Parser Coverage (PCA/SIP/EBT)". | 3 | DEFERRED |

**New follow-ups ticketed 2026-06-06 (Tech-Debt queue):** **B-121** (DIR-60, PCA/corporate-holder parser coverage — the real fix behind the B-020 orphans), **B-119** (DIR-61, backfill prices for `no_market` rows then re-reconcile to *correct*), **B-120** (DIR-62, "unverified value" marker on price-audit-flagged rows).

**Acceptance criteria**
- B-112: trailing-12 sums correct for a fixture with a known month boundary; null-£ rows excluded from £ totals but **counted in volume** (no zero-fill); trend glyph (▲/▼) sign correct when this-month > / < last-month; "n/a" shown until ≥13 months of data exist; clicking a block opens the correct txns.
- B-113: score ordering matches expectation for fixtures with known director-count / £ / compression; if board size is unavailable, score on the three available fields and note the omission (don't block); formula is transparent/documented, not a black box.

**Out of scope:** any change to cluster *definition* (S1 stays 2+ distinct directors, all BUYs, within 30 days); new signal_ids (the cluster score is a display chip, not a fired signal id).

**Rupert deploy sequence:**
```powershell
python .scripts\export_dashboard_json.py
python .scripts\build_dashboard.py
```

### Sprint 36 — Dependent + heavier items (earnings multiplier, conviction sizing, push digest)

Goal: the items that depend on earlier work or sit off the render path. B-114 is sequenced last because it **depends on B-111** (Sprint 34) landing first.

**Sprint 36 — ✅ COMPLETE 2026-06-06.** B-115 ✅ (conviction sizing); **B-114 ✅** built (forward-only pre-earnings conviction chip + This-Week filter in render_index — no new signal_id / eval rebuild / lookahead, per Rupert's forward-only call; `TestB114PreEarningsChip`); **B-116 ✅** built (`morning_digest.py` — dry-run default, env-var SMTP, freshness guard, pre-earnings highlight; `test_morning_digest.py`; needs Rupert's Gmail app-password env + Task Scheduler to go live); B-118 (est-filler) deferred → Backlog (DIR-41). Pending Rupert's Windows full sweep.

| ID | Item | Size |
|----|------|------|
| **B-114** 🔨 | Earnings-proximity signal multiplier — pre-earnings window (buy 0–30d before a confirmed results date) attaches a separately-scored "pre-earnings" conviction flag (prefer per-bucket granularity over tier-bump). **UNBLOCKED — B-111 done.** Build map: new module in `.scripts/signals/`, register in `signals/__init__.py`, read `reporting_dates` (confirmed only) gated for lookahead, add to `test_p3_lookahead.py`, three-layer signal_id (render_helpers SIGNAL_DISPLAY_ORDER + export SIGNAL_ORDER/SIGNAL_SHORT + JS SIDS templated), diff-first eval rebuild. | M |
| **B-118** ⏭ | Synthetic "(est)" gap-filler `backfill_expected_reporting_dates.py` (DIR-41) — landing zone (migration 009 `confidence='est'`) ready; deferred. | M |

---

## Sprint 55 — Basket Validation Discipline + AIM Benchmark Fix — ✅ COMPLETE 2026-06-07

| ID | Item | Status |
|----|------|--------|
| **B-141** ✅ | Basket validation discipline doc (`docs/specs/basket-validation-rules.md`). Pre-registration rules, n≥30 gate, median headline, benchmark/cost spec, regime caveat, forward paper-book, £500m threshold. DIR-68. | Done 2026-06-07 |
| **B-147** ✅ | AIM benchmark fix — 95% of small-cap firings were using `^FTAS` (Main Market) instead of `^FTSC` (FTSE Small Cap). Fixed in backtest pipeline. DIR-74. | Done 2026-06-07 |

---

## Sprint 56 — Performance Page Split: Small Cap / Large Cap (2026-06-07)

Goal: replace the single `performance.html` with two size-band-specific pages. All charts, stats, scoreboard, strategy tracker, cohort cuts, and paper book on each page show only that size band's data. Nav becomes `Today | Small Cap | Large Cap | All | Baskets | Review`.

**Key fact:** `_backtest_results.csv` already has a `small_cap` column — pre-filtering rows is a one-liner. No schema changes, no Zone-B scripts. Pure Zone A build.

**Decisions locked 2026-06-07:**
- Keep combined `performance.html`, rename nav link to "All"
- Nav labels: "Small Cap" and "Large Cap"
- Strategy tracker filter built in Sprint 56 (not deferred)

| ID | Item | Size |
|----|------|------|
| ✅ **B-149** / DIR-77 | Performance page split — 5 phases: (A) export layer writes `signals_small.json` + `signals_large.json`; (B) `build_strategy_tracker` gains `small_cap` filter param; (C) `render_performance` gains `size_band` param for title/subtitle; (D) `build_dashboard` wires two new render calls + nav links; (E) operator workflow guide + `manual_classify --check` flag. Post-ship fix: `build_monthly_buysell` + `cohort_performance_{band}.json` split for signal-overview mini-charts. Done 2026-06-07. | L (8) |
| ✅ **B-150** / DIR-78 | New company classification workflow doc (`docs/guides/new-company-workflow.md`) — folded into B-149 Phase E. Done 2026-06-07. | S (1) |
> **Note:** B-148 (DIR-76) was already used in Sprint 55 (market-cap pipeline completeness).

**Deploy sequence (after build):**
```powershell
python .scripts\export_dashboard_json.py   # writes signals_small.json + signals_large.json
python .scripts\build_dashboard.py         # writes performance_small.html + performance_large.html
python .scripts\snapshot_db.py
```

---

## Sprint 53 — Predictive Edge: Market-Cap Enrichment + Basket Report (2026-06-07)

The pivot from **descriptive** (what trades worked) to **predictive** (what will work). Triggered by Rupert's product-brainstorm 2026-06-07. Full scope + data: `docs/specs/predictive-edge-2026-06-07-plan.md`. Linear milestone "Sprint 53 — Predictive Edge", issues **DIR-64 → DIR-69**.

**Key finding:** the flagship thesis — conviction chairman buys in small-caps — is **unmeasurable today** (Chairman small-cap basket n=1, Conviction chairman small-cap n=0). Root cause: market cap known for only 31/605 tickers (all large), 24/1,264 firings AIM-tagged. Yahoo doesn't return market cap for `.L`. So market-cap enrichment is the critical path.

**Success basis (Rupert):** judge baskets on **net CAR** (beat the benchmark, net of costs), not absolute return. Carry a standing regime caveat (market surged → CAR negative across the board) and use **median** not mean.

| ID | Linear | Item | Pri | Pts | Dep |
|----|--------|------|-----|-----|-----|
| B-137 | DIR-64 | Market-cap & shares-in-issue enrichment (lse.co.uk scrape; `backfill_market_cap.py`) — **blocker** | P1 | 5 | — |
| B-138 | DIR-65 | Small-cap classification + basket filter inputs | P2 | 3 | B-137 |
| B-139 | DIR-66 | Backtest re-run + validate baskets populate (AIM benchmark fix) | P2 | 3 | B-137/138 |
| B-140 | DIR-67 | Basket Report production page (`render_baskets.py` + nav + nightly build) | P2 | 5 | B-139 |
| B-141 | DIR-68 | Basket validation discipline (pre-registration + n≥30 gate) | P3 | 2 | — |
| B-142 | DIR-69 | Automate daily LSE diary scrape (`daily_diary_scrape.bat` shipped; schtasks) | P2 | 1 | — |
| ✅ B-143 | DIR-70 | Duplicate transaction detection + removal — exact fingerprint dedup + near-dedup (same director/value, date ±7 days). DB: 6,454→6,338. **DONE 2026-06-07** | P2 | 3 | — |
| ✅ B-144 | DIR-71 | Strategy tracker: T+1/T+21/T+90/T+252 horizon toggle + per-signal chip toggles (11 signals). **DONE + DEPLOYED 2026-06-07** | P2 | 3 | — |

**Data-source decisions (research 2026-06-07):** shares/market cap → scrape lse.co.uk SharePrice meta tag (verified on AIM micro-cap), fallback RNS Total Voting Rights via Investegate; forward earnings → keep lse.co.uk diary, gap-fill via Investegate "Notice of Results". No paid API needed.

**Already done this session (Zone A):** dashboard-designer page spec, standalone preview `outputs/baskets.html` (ranked by net CAR), `daily_diary_scrape.bat`.
| **B-115** ✅ DONE 2026-06-05 | Buy size vs skin-in-the-game — **re-scoped to absolute-£ conviction sizing** (prior-holding ratio not buildable: no holding data in `transactions`; deferred). New `.scripts/sizing.py` `position_size()` (log default / tier / linear / flat, £500 floor, £5k cap per spec 07 D1–D3); wired into `eval_signals._open_paper_trade` + `--sizing`/`--max-notional` CLI; read-path cap 50k→5k. Flat-vs-conviction comparison = existing B-078 VW toggle. 1194 tests green. | M |
| **B-116** | Morning push digest — 7am email of overnight buys to **rspiegelberg@gmail.com** (locked); gate on export freshness; dry-run mode. | M |

**Acceptance criteria**
- B-114: **lookahead-bias test (P3-6) passes** — the multiplier uses only information available at transaction time. A **diff-first report** (firing-count delta) is produced and reviewed *before* any signal logic changes merge (phase-gated). New flag id honours the **three-layer rule**: Python render constants + export `SIGNAL_ORDER` + JS `SIDS` all updated.
- B-115: each buy carries a size-vs-prior-holding ratio; matches spec 07's definition; small CEO buys (e.g. £5k) read as noise, large (£500k) as conviction. If spec 07 introduces a new signal_id, three-layer rule applies.
- B-116: dry-run prints the digest without sending; "no new signals" path produces a sensible (or suppressed) email; digest is **not sent when the morning pipeline failed** (stale-data guard on export freshness).

**Out of scope:** B-114 must not silently change historical firing counts (the diff report is the gate); F's send mechanism is unresolved (see open questions) — settle it before building B-116.

**Rupert deploy sequence:**
```powershell
# B-114 — rebuild signals with the new pre-earnings flag, then redeploy:
python .scripts\eval_signals.py --rebuild
python .scripts\export_dashboard_json.py
python .scripts\build_dashboard.py
# B-115 per spec 07 (its own backfill/eval step if introduced).
# B-116 runs off the freshest dealings.json AFTER the morning pipeline — send mechanism TBD.
```

### Open questions for Rupert (Sprints 33–36)

1. **F — send mechanism:** in-assistant scheduled task vs a standalone Windows-scheduled script? If standalone, settle the gmail SMTP / app-password setup. *(Blocks B-116 build.)* — **still open.**

### B-111 decisions — LOCKED 2026-06-05

- **Source:** lse.co.uk Financial Diary, scraped by-date (`source='lse_diary'`). Investegate "Notice of Results" RNS kept only as a Phase-B fallback if Diary gaps prove material.
- **Cadence (was Q2):** **once a day** (daily scheduled run).
- **Estimate fallback (was Q4):** **allow a synthetic estimate**, but it MUST be marked as an estimate — `confidence='est'` and a visible "(est)" suffix on the badge. Confirmed dates carry `confidence='confirmed'` (no suffix). So a synthetic date can never read as confirmed.
- **report_type (was Q3):** **keep the existing four** (`INTERIM`, `FINAL`, `TRADING_UPDATE`, `EARNINGS`) **and add three** (`PRELIM`, `QUARTERLY`, `TRADING_STMT`). Migration 009 widens the `reporting_dates.report_type` CHECK constraint to the full seven and adds the nullable `confidence` column (default `'confirmed'` for existing rows).

---

## 9. THIS IS THE CANONICAL ROADMAP

**Consolidated 2026-06-06.** This file (`roadmap-2026-06-05.md`) is the single source of truth. It **supersedes and absorbs `roadmap-2026-06-06.md`** (which was an explicit increment on this base, not a competing doc). The product-fixes batch from that file is merged below. Detailed per-item diagnoses/designs live in `docs/specs/product-fixes-2026-06-06-plan.md`.

### Sprints 50–52 — product-fixes batch (B-122 → B-133), merged from roadmap-2026-06-06.md

> ✅ **On the board (corrected 2026-06-06):** these issues DO exist — DIR-45 (B-129) … DIR-57 (B-134) etc., with milestones Sprint 50/51/52 + Sprint 38. An earlier issue-list call falsely returned `hasNextPage:false` with only 25 rows, which led to a momentary "missing" misread and 3 duplicate creates (DIR-60/61/62) that were then **cancelled as duplicates**. No recreation needed; the board reflects Sprints 50–52. (Sprint 50 = Done; Sprint 51 partly shipped; Sprint 52 backlog.)

| B-ID | (claimed) Linear | Item | Sprint | Pri | Pts | Status |
|------|--------|------|--------|-----|-----|--------|
| B-129 | DIR-45 | Review "Apply & publish" stuck (stale apply-status lock) | 50 | Urgent | 3 | ✅ shipped (code) |
| B-131 | DIR-46 | Published review items don't drop off review page | 50 | High | 1 | ✅ shipped |
| B-122 | DIR-47 | "Signals Today" undercounts current-day signals (KPI fire-date bucket) | 50 | High | 1 | ✅ shipped |
| B-133 | DIR-48 | Company-page BMK column always blank | 50 | High | 1 | ✅ shipped |
| B-126 | DIR-49 | Cohort table: relabel CAR vs Net-of-Costs | 50 | Med | 1 | ✅ shipped |
| B-127 | DIR-50 | Cohort table: abs stock-return from announcement | 50 | Med | 2 | ✅ shipped |
| B-123 | DIR-51 | £10k-per-signal strategy tracker vs FTSE All-Share | 51 | Med | 5 | ✅ Done (already-built, verified 2026-06-06) |
| B-124 | DIR-52 | Paper-book signal filter — row badge as live filter | 51 | Med | 2 | ✅ Done |
| B-125 | DIR-53 | Monthly Activity: remove corporate/PCA sell volume | 51 | High | 2 | ✅ Done |
| B-128 | DIR-54 | Sector data source: populate sector per stock | 52 | Med | 3 | rescoped → alt ticker-keyed source (Yahoo blocked) |
| B-130 | DIR-55 | Review form: prefill edit fields with parser best-guess | 52 | Med | 2 | Todo — best-guess passover approved |
| B-132 | DIR-56 | Cluster-brewing trend — count + 8-week sparkline | 52 | Med | 3 | ✅ Done (built 2026-06-06) |

Plus apply-pipeline fixes **B-134 / B-135** (per memory `project_sprint_50_shipped.md`). Locked decisions for this batch: tracker = flat £10k/buy-signal, enter at announcement, sell at T+21, value trailing-30d vs an identical £10k/signal `^FTAS` shadow; paper-book filter = the row signal badge; Monthly Activity removes corporate/PCA sell volume; cohort abs-return measured from announcement; cluster-brewing trend = count of brewing clusters vs 30-day avg + 8-week sparkline.

**`roadmap-2026-06-06.md` is now a stub pointing here.**

### 9a. Backlog-clear review — decisions locked 2026-06-06

> **✅ STATUS 2026-06-06 (evening): ALL DEPLOYED + VERIFIED.** B-118, B-120, B-132, B-090C, B-121, B-130, B-136, B-128 all built, deployed and verified. Reparse landed transactions 6,089→6,454 (+203 SIP, +56 BUY), signals 2,963→3,057; B-136 corporate exclusion live (arms-length corps fire 0, PCAs/family trusts kept); B-130 wrote 2,132 review prefills; B-128 wrote 212/511 sectors (rest fills over the next 1–2 daily runs). **DIR-18/41/43/44/51/54/55/56/63 = Done.** **B-119 (DIR-42) PARKED** (Rupert) — data-availability-limited; display already mitigated by the B-120 unverified chip. Gated B-078ph/080/081/082 cancelled. The decision/build notes below are retained for history; their deploy-gating caveats are now satisfied.

A full backlog-clear pass on 2026-06-06 cleared **B-123** (verified already-built), **B-118**, **B-120**, **B-132** (built + tested) and **B-090C** (parser built + independently QA-passed). Remaining decisions locked with Rupert:

- **Scope principle (NEW — B-136 / DIR-63, High):** scoring is limited to **straightforward BUYS & SELLS by individual directors and related individuals (PCAs)** — NOT corporates, NOT SIP / award / grant / nil-cost / EBT mechanics. Parsed data is still STORED (completeness) but EXCLUDED from the CAR/performance tracker, per-share return columns, cohort/strategy payloads, and signal firing. Single "scoreable transaction" predicate across `eval_signals` / `backtest` / `export_dashboard_json`.
- **B-090C (DIR-18):** SIP multi-tranche parser BUILT + QA-passed — recovers GPE-style filings via the `d) Aggregated information` row (Courtauld 147/£150.75, Sanderson 144/£147.66, Nicholson 147/£150.75). Deploy gated on (a) Windows fixture run = 23 green, (b) B-136 SIP-exclusion (SIP rows carry a blended cost basis → spurious ~+200% per-share returns).
- **B-121 (DIR-44):** RESCOPED — **exclude** corporate / corporate-PCA holders (the "accept corporate as director" idea is dropped); **recover** wrongly-dropped INDIVIDUAL filings (false-bundled gate fix). Needs the Windows fixture harness; header-regex sub-claim is imprecise (regex already matches "person closely associated").
- **B-128 (DIR-54):** Yahoo `assetProfile` blocked → find an ALTERNATIVE ticker-keyed sector source (FMP `/profile/{TICKER}.L` candidate) and backfill.
- **B-130 (DIR-55):** best-guess prefill passover APPROVED — deterministic `parse_pdmr` first, LLM only for residuals, confirm budget before a full run.
- **Gated items ERASED:** B-078 Phase B / B-080 / B-081 / B-082 (DIR-36/37/38/39) **cancelled** in Linear — revisit only if needed later. §2 and §7 below are superseded on this point.

**Deploy now (no parser dependency):** `backfill_expected_reporting_dates.py` (B-118) → `export_dashboard_json.py` → `build_dashboard.py` → `snapshot_db.py` → `python -m unittest discover -s .scripts -p "test_*.py"`. **Hold `reparse_corpus`** (B-090C) until B-136 SIP-exclusion ships, else the dashboard publishes spurious SIP returns.

---

## §10. Sprint 53 close-out + Sprint 54 — 2026-06-07

### Sprint 53 — Predictive Edge: Market-Cap Enrichment + Basket Report

> **STATUS 2026-06-07:** Partial close. 3 of 9 items shipped; the market-cap data chain (B-137→B-140) remains unexecuted — B-137 is the P1 blocker. Unexecuted items carried forward to Sprint 54.

| B-ID | DIR | Item | Status |
|------|-----|------|--------|
| B-142 | DIR-69 | Automate daily LSE diary scrape (`daily_diary_scrape.bat` + schtasks) | ✅ Done 2026-06-07 |
| B-143 | DIR-70 | Dedup transactions — 6,454 → 6,338 (exact-match fingerprint sweep) | ✅ Done 2026-06-07 |
| B-144 | DIR-71 | Strategy tracker toggles (toggle by signal tier on tracker page) | ✅ Done 2026-06-07 |
| B-137 | DIR-64 | Market-cap & shares-in-issue enrichment (lse.co.uk scrape) | → Sprint 54 |
| B-138 | DIR-65 | Small-cap classification + basket filter inputs | → Sprint 54 |
| B-139 | DIR-66 | Backtest re-run + validate small-cap/chairman/PCA baskets | → Sprint 54 |
| B-140 | DIR-67 | Basket Report production page | → Sprint 54 |
| B-141 | DIR-68 | Basket validation discipline | → Sprint 54 |
| B-145 | DIR-72 | Upcoming Events box on This Week page | → Sprint 54 |

---

### Sprint 54 — Predictive Edge: Market-Cap + Basket Report (Continued) ✅ CLOSED 2026-06-07

**Linear milestone:** `Sprint 54 — Predictive Edge: Market-Cap + Basket Report (Continued)` (ID: `7bd00779-6a6a-4909-93c7-023acf2cec08`)
**Cycle:** 3 (2026-06-08 → 2026-06-10)
**Total estimate:** ~20 pts

> **STATUS 2026-06-07 — CLOSED.** 7 of 9 items Done. B-141 (basket validation doc, P3) deferred to Sprint 55 Backlog. DIR-74 (B-147 follow-up — widen is_aim coverage, root cause of misleading small-cap basket CAR) opened during sprint → Sprint 55 P1.

**Critical path:** B-137 → B-138 → B-139 → B-140 (must execute in order)
**Parallel start:** B-145 (Zone A only — in progress), B-141 (docs/config — standalone)

| B-ID | DIR | Item | Pri | Pts | Agent | Dep | Status |
|------|-----|------|-----|-----|-------|-----|--------|
| ✅ B-137 | DIR-64 | Market-cap & shares-in-issue enrichment — `backfill_market_cap.py` scraping lse.co.uk `Shares in Issue` + `Market Cap` from meta-description tag; TTL-cached; `market_cap_gbp = shares × latest close`. **Done 2026-06-07** | P1 | 5 | data-integrity-auditor | — | ✅ Done |
| ✅ B-138 | DIR-65 | Small-cap classification — schema migration 011 adds `small_cap` flag (<£500m); classify_small_cap.py; propagated to backtest. **Done 2026-06-07** | P2 | 3 | data-integrity-auditor | B-137 | ✅ Done |
| ✅ B-139 | DIR-66 | Backtest re-run — eval_signals + backtest re-run with enriched data; AIM benchmark fixed (^AIM→^FTSC, B-147); baskets populate. **Done 2026-06-07** | P2 | 3 | general-purpose | B-137/138 | ✅ Done |
| ✅ B-140 | DIR-67 | Basket Report production page — `baskets_config.json` + `render_baskets.py` + `export_baskets_json.py`; ranked by median net CAR; n<30 gate; nav + nightly build. **Done 2026-06-07** | P2 | 5 | dashboard-designer | B-139 | ✅ Done |
| ✅ B-147 | DIR-75 | AIM benchmark fix — ^AIM delisted on Yahoo; replaced with ^FTSC (FTSE Small Cap) across 6 files + benchmark_symbols.json. 38/38 tests green. **Done 2026-06-07** | P1 | 2 | general-purpose | — | ✅ Done |
| ✅ B-141 | DIR-68 | Basket validation discipline — pre-registration rule + n≥30 gate + median headline + regime caveat doc. **Done Sprint 55 2026-06-07** | P3 | 2 | general-purpose | — | ✅ Done |
| ✅ B-145 | DIR-72 | Upcoming Events box on This Week page — `export_dashboard_json.py` emits `upcoming_events` (T+0→T+14 from `reporting_dates`); `render_index.py` renders panel. **Done + deployed 2026-06-07** | P2 | 2 | dashboard-designer | — | ✅ Done |
| ✅ B-146 | DIR-73 | Market-cap column on all ticker-bearing views — add £Mkt Cap column to main dealings table, performance tracker, company drill-down header, and Upcoming Events box; format as `£214.9m` / `£1.2bn`; `—` for NULL | P2 | 3 | dashboard-designer | B-137 | ✅ Done 2026-06-07 |

**Decisions locked (Sprint 53/54):**
- Success basis = beat benchmark net CAR (Rupert 2026-06-07)
- Small-cap threshold = <£500m market cap (confirmed 2026-06-07)
- Basket headline = median net CAR (not mean — outlier-driven)
- n<30 → "not proven" label on Basket Report
- Forward paper-book is the honest validation; backtest is for calibration only

---

## §11. Sprint 55 — Basket Reliability: AIM Benchmark + Validation (2026-06-07)

**Linear milestone:** `Sprint 55 — Basket Reliability: AIM Benchmark + Validation` (ID: `859796a0-10f3-4476-beab-b168b94e1232`)
**Target:** 2026-06-14

**Context:** The Basket Report (B-140) is live but small-cap basket CAR numbers are misleading. Root cause: `is_aim` covers only 42 of 605 tickers, so 95% of small-cap firings (713/749) are benchmarked against ^FTAS (FTSE All-Share, large-cap dominated) instead of ^FTSC (FTSE Small Cap). This artificially depresses all small-cap basket returns relative to their actual benchmark and makes the signal appear to have no edge when it may well have one.

| B-ID | DIR | Item | Pri | Pts | Agent | Dep | Status |
|------|-----|------|-----|-----|-------|-----|--------|
| ✅ B-147 | DIR-74 | **AIM benchmark coverage fix** — widen `is_aim` classification (target ≥100 AIM tickers vs current 42); set `benchmark_symbol='^FTSC'` for all AIM tickers; backfill ^FTSC prices if missing; re-run backtest so basket CAR is meaningful. Zone A (script edits) + B (Rupert runs backfill + backtest). **Done 2026-06-07** | P1 | 4 | data-integrity-auditor | — | ✅ Done |
| ✅ B-148 | DIR-76 | **Market-cap pipeline completeness** — `backfill_market_cap.py --missing-only` flag; `refresh_all.py` wiring (mktcap + smallcap steps before signals); holding-basket counter on dashboard index (amber/green strip); `manual_classify.py` Zone B script with hard-coded caps for 19 unresolvable tickers (10 Yahoo-confirmed + 9 manual research). **Done 2026-06-07** | P2 | 2 | data-integrity-auditor | — | ✅ Done |
| ✅ B-141 | DIR-68 | **Basket validation discipline** — pre-registration rule + n≥30 gate + median headline + regime caveat doc. `docs/specs/basket-validation-rules.md`. Threshold updated throughout to <£500m. **Done 2026-06-07** | P3 | 2 | general-purpose | — | ✅ Done |

**Decisions to lock before B-147 build:**
- AIM detection source: lse.co.uk SharePrice page already fetched by `backfill_ticker_meta.py` — confirm exchange field reliably identifies AIM vs Main Market
- Benchmark for Main-Market small-caps (small_cap=1 but is_aim=0): keep ^FTAS — defensible (they chose a Main Market listing)
- After fix: re-run `backtest.py` → `export_baskets_json.py` → `build_dashboard.py`

**Acceptance:** <10% of small_cap firings benchmark against ^FTAS after fix; Basket Report small-cap basket CAR reflects genuine small-cap vs ^FTSC performance, not small-cap vs large-cap.

---

## §12. Backlog — Unscheduled (post Sprint 56)

| B-ID | DIR | Item | Pri | Pts | Agent | Dep | Status |
|------|-----|------|-----|-----|-------|-----|--------|
| B-151 | DIR-82 | **Paper P&L tile: wire signal-tier filter + add time-horizon toggle** — (1) The signal-tier dropdown currently saves a localStorage pref but never recalculates P&L — wire it up so selecting a tier filters the £ total to matching open trades; (2) Add T+1/T+21/T+90 horizon toggle; (3) Relabel "T1" option to "T1 — CEO/CFO/Founder" to avoid confusion with T+1 horizon. Touches `render_index.py` + `export_dashboard_json.py`. | P3 | 3 | dashboard-designer | — | Backlog |

> ⚠️ Numbering note (2026-06-10): the row above is now **B-153** in Linear (DIR-82) — it was renumbered to make room for the B-151 horizon rename (DIR-83, ✅ Done 2026-06-09). See `docs/backlog.md`.

---

## §13. Alpha-research data points — 2026-06-10 batch (B-155 → B-164)

**Source:** quant + trader agent review, `docs/research/alpha-research-2026-06-10.md`.
**Headline:** no positive factor combination exists in the current data (all 49 robust
interaction cells negative — usable only as an avoid-filter: small/mid-cap × CFO ×
cluster are the worst losers). PCA hypothesis rejected (-4.3% net CAR T+90, n=70,
indistinguishable from the rest). Path to alpha = new conviction data points + honest
cost model + longer price history — not more slicing of existing fields.

All 10 on the Linear board (DIR-86 → DIR-95), state Backlog. Mirrored in `docs/backlog.md`.

| B-ID | DIR | Item | Pri | Agent | Dep |
|------|-----|------|-----|-------|-----|
| B-157 | DIR-88 | **Corwin-Schultz dynamic per-stock spread cost replacing flat cost_bps** — correctness fix, flat cost flatters AIM backtests (real AIM spreads 5–10%). Diff-first; rerun backtest→eval→export→build. | P1 | general-purpose | — |
| B-158 | DIR-89 | **Sector coverage backfill 24% → >80%** — #1 data gap; fixes sector-matched benchmarking (extends B-147/DIR-74 theme). | P1 | data-integrity-auditor | — |
| B-155 | DIR-86 | Routine vs opportunistic trader flag (Cohen-Malloy-Pomorski; computed from own history). | P2 | general-purpose | better after B-162 |
| B-156 | DIR-87 | Parse "resulting holding" from RNS → % increase in director's stake (#2 data gap; schema migration + reparse). | P2 | data-integrity-auditor | — |
| B-159 | DIR-90 | Net-seller-reversal flag (extends F1 first-buy). | P2 | general-purpose | better after B-162 |
| B-160 | DIR-91 | 52-week low/high distance + momentum features from existing OHLCV (direction contested — measure, don't pre-judge). | P2 | general-purpose | better after B-162 |
| B-161 | DIR-92 | "First window after results" flag (UK MAR timing — buys cluster post-results). | P2 | general-purpose | reporting_dates coverage |
| B-162 | DIR-93 | Extend price + benchmark history backward 3–5 yrs (Yahoo) — fixes the 1-year-window caveat; strengthens B-155/159/160. | P2 | data-integrity-auditor | — |
| B-163 | DIR-94 | Director salary multiple (annual-report scrape) — feasibility spike first; may be redundant after B-156. | P3 | general-purpose | B-156 verdict |
| B-164 | DIR-95 | Short interest ingestion (FCA daily CSV, ≥0.5% disclosures only). | P3 | general-purpose | — |

**Suggested sequencing:** Sprint N = B-157 + B-158 (correctness first — every backtest
until then is measured against a flattering cost model and generic benchmark);
Sprint N+1 = B-162 + the free feature flags (B-155/159/160/161); B-156 parser build
alongside; B-163/B-164 only after the first re-scan shows where the edge is.

---

## §11 — Cloud front page + pipeline reliability (added 2026-06-23)

**Context.** The cloud migration (Supabase + Vercel + GitHub Actions) is live; company
pages read the DB directly in the browser and work perfectly. But the **front page** and
the **daily auto-refresh** hit a hard wall: the pipeline's heavy compute
(`eval_signals` >45min, `backtest` >60min) is **too slow on US GitHub runners against
eu-west-1 Supabase** (thousands of small queries × transatlantic latency). The
"pipeline renders HTML → publishes to `rendered_pages` → shell" approach is **abandoned**.

**Decision (Rupert, 2026-06-23):** rebuild the front page as a **live, client-side
direct-read page** like the company pages. Full spec: `docs/specs/live-front-page-spec.md`.
Tracked in `cloud-migration-sprint-tracker.md` as **Sprint M6** + **Sprint DR**.

| B-ID | Item | Pri | Status (synced to Linear 2026-06-25) |
|------|------|-----|--------|
| B-190 | Live front + company pages — Supabase direct-read in the browser; real graded conviction panel; company.html parity (badges, earnings markers, links, type filter, chart name tooltips) | P1 | ✅ Done 2026-06-25 (DIR-127) |
| B-191 | Conviction panel + capital-deployed charts — **folded into B-190** (shipped on the same live page) | P1 | ✅ Done 2026-06-25 (in DIR-127) |
| B-192 | Live front page polish — brewing-cluster sparklines, mobile layout, paper P&L | P2 | ⏳ Open — optional enhancement, not blocking |
| B-193 | Remove dead publish path (`_publish_live_index` / `_live_shell_html` from build_dashboard.py + Supabase `rendered_pages` table/view) | P1 | ✅ Done 2026-06-25 (DIR-129) |
| B-194 | Daily-refresh re-architecture — download Supabase→local SQLite on the runner, compute in-process (`DD_FORCE_SQLITE`), upload changed tables back. ~6 min vs 30–47 min timeouts. Git push made race-proof (5-retry `rebase -X theirs`) after run #8 exit-128 | P1 | ✅ Done + LIVE 2026-06-25 (DIR-128) |
| B-195 | Failure alert if a scheduled run fails | P2 | ✅ Done — free via GitHub's built-in Actions-failure email notifications (no token/code) |
| B-196 | Missing real BUYS (CTA/KLSO/GANA). **CORRECTED diagnosis: NOT discovery, NOT exclusion** — they're discovered + fetched but the PARSER dropped them on non-standard RNS layouts ("Name of entity"/"Full name of person Dealing" labels, nested `Price (p)`/`Volume(s)` with pence-in-header, inline price/vol, sub-penny). Fixed `parse_pdmr.py` (`_extract_via_aggregate_table`); all 3 ingested to Supabase | P1 | ✅ Done + LIVE 2026-06-25 (DIR-130) |
| B-197 | Manual ↻ Refresh button on the live page → Supabase edge fn `trigger-refresh` (5-min cooldown; token = Supabase secret `GITHUB_TOKEN`, server-side only) dispatches the daily-refresh workflow | P3 | ✅ Done + LIVE 2026-06-25 (DIR-131) |

**Status (2026-06-25):** M6 + DR shipped and live. The cloud pipeline self-refreshes
(~6 min daily + a manual refresh button), the front/company pages read Supabase directly,
and the three missing buys are in. **Remaining open:** B-192 (optional polish) and the
**M5 decommission** pair — B-188 (retire the FUSE/two-zone notes from CLAUDE.md, DIR-117)
and B-189 (archive the local DB, DIR-118). **Do NOT archive local files (M5/B-189) until
the daily refresh has run unattended cleanly for ~a week** and Rupert gives the go-ahead.
